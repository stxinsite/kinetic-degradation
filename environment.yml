name: kinetic-degradation
channels:
  - defaults
dependencies:
  - python[version='>=3.9']
  - scipy
  - seaborn
  - pip
  - conda-forge::numdifftools
  - matplotlib[version='>3.4']
  - numpy
  - pyyaml
  - pandas
prefix: /projects2/insite/daniel.park/anaconda3/envs/kinetic-degradation
